CREATE TABLE USER (ID INTEGER AUTO_INCREMENT NOT NULL, DTYPE VARCHAR(31), AGE INTEGER, NOM VARCHAR(40), USERS_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE CUSTOMER (ID INTEGER NOT NULL, ref_cust VARCHAR(10) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE USERS (ID INTEGER AUTO_INCREMENT NOT NULL, PRIMARY KEY (ID))
ALTER TABLE USER ADD CONSTRAINT FK_USER_USERS_ID FOREIGN KEY (USERS_ID) REFERENCES USERS (ID)
ALTER TABLE CUSTOMER ADD CONSTRAINT FK_CUSTOMER_ID FOREIGN KEY (ID) REFERENCES USER (ID)
